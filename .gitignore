/vendor/
/build/
/node_modules/
/.playwright-mcp/
/.claude/
/.claude/worktrees/
# Design reference (React prototype + screenshots) — kept local, not shipped.
# Reversible: remove this line to version it.
/heisenberg-ui/
# Local AI-tool state — never versioned.
.freebuff/
.hermes/
composer.lock
# Dev-server (testbench serve) sqlite DB — created by `testbench migrate --seed`.
/database/*.sqlite
/storage/
.phpunit.result.cache
.phpunit.cache/
phpunit.xml
.idea/
.vscode/
.fleet/
*.cache
*.log
.DS_Store
Thumbs.db
tests/Builder/js/check-braces.js
tests/Builder/js/check-syntax.js

# Local secrets (SMTP credentials etc.) — never versioned.
.env
.env.*
!.env.example
