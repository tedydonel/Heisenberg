# Dev-server (testbench serve/demo:seed) sqlite DB — see testbench.yaml
# DB_DATABASE and docs/demo.md. Regenerated by `php vendor/bin/testbench
# migrate:fresh` + `php vendor/bin/testbench demo:seed`; never versioned.
/database/*.sqlite
/database/*.sqlite-journal
